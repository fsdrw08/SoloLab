# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

# https://atmos.tools/migration/native-terraform
name: test-db

import:
  - _defaults

components:
  terraform:
    test-db:
      metadata:
        component: Nomad/Jobs/postgresql
      vars: !include ../../TerraformWorkShop/Nomad/Jobs/postgresql/terraform.tfvars
