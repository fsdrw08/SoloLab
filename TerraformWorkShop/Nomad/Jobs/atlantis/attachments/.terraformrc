provider_installation {
  network_mirror {
    url = "https://dufs.day1.sololab/public/mirror/terraform/"
    include = ["registry.terraform.io/*/*"]
  }
  direct {
    exclude = ["registry.terraform.io/*/*"]
  }
}
plugin_cache_dir   = "$HOME/.atlantis/plugin-cache/"
disable_checkpoint = true