# code: language=ansible
---
vars_podman_play:
  kube_file: /KubeWorkShop/FreeIPA/aio-freeipa.yaml
  state: absent # absent / created / started
  force: true
  # env_var:
  #   PODMAN_USERNS: keep-id:uid=1000,gid=1000
vars_podman_generate_systemd:
  name: freeipa
vars_service:
  podman_pod:
    user_scope:
      skipped: false
      enabled: true
      state: started